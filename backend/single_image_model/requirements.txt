# Install a CUDA-compatible PyTorch build for the deployment host first when needed.
torch>=2.1
torchvision>=0.16
transformers>=4.45,<5
peft==0.19.1
accelerate>=0.34
safetensors>=0.4
Pillow>=10
einops>=0.7
timm>=1.0
